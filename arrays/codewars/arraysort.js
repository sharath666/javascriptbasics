array =[1,12]
result = []
result.push((Math.max(...array)))
result.push((Math.min(...array)))


// result = []
// if (array.length ==1 )
// {
//     result.push(array[0])
//     result.push(array[0])
//     return result
// }
// min = array.sort((b,a)=> b - a).slice()
// max = array.sort((a,b) => b - a).slice()

// console.log(min[0])
// console.log(max[0])

// result.push(min[0])
// result.push(max[0])

// return result